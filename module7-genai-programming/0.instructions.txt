1. Install uv (pthon env/package manager): 
        pip install uv (OR)
        (windows) irm https://astral.sh/uv/install.ps1 | iex
2. Install python 3.12 (dont use 3.13/3.14 right now): uv python install 3.12
3. uv venv .venv-langchain --python 3.12
4. Activate the virtual environment: uv venv .venv-langchain --activate
5. Install required packages: uv pip install -r requirements.txt
6. Install LLMs Locally (windows): 
                irm https://ollama.com/install.ps1 | iex  
        (OR)    https://lmstudio.ai/download/latest/win32/x64
7. Download models for local testing (windows):
        ollama pull llama3:8b
        ollama pull qwen2.5:7b
8. uv pip install chromadb
----------------------------------------
1. go to langsmith.com and create an account.
2. Create a new project and get the API key.
3. Set the API key as an environment variable: export LANGCHAIN_HANDLER=langsmith
4. Run the code to test the integration with LangSmith.